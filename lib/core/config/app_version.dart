/// نسخة التطبيق كما في pubspec.yaml.
///
/// تُرسل مع كل طلب في ترويسة `X-App-Version` ليقارنها السيرفر بأدنى نسخة
/// يسمح بها المشرف. ثابت مكتوب لا حزمة package_info: الحزمة تضيف قناة منصّة
/// لكل نظام من أجل نص واحد، واختبار `app_version_test` يفشل إن نُسي تحديث
/// هذا السطر مع pubspec.
const String appVersion = '0.1.0+1';
